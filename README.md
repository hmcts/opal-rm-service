# opal-rm-service

Opal Reciprocal Maintenance service.

## Running locally

Build the service:

```bash
./gradlew build
```

Start the service:

```bash
./gradlew run
```

Check health:

```bash
curl http://localhost:4556/health
```
